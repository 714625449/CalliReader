# CalliReader vs CaoshuReader 整页 OCR 评估报告

> 日期：2025-05-08  
> 环境：RTX 3090 24GB / PyTorch 2.5+ / Python 3.13  
> 基础模型：InternVL2.5-8B  
> CaoshuReader 训练配置：PerceiverResampler(8 layers, 12 learns, dropout 0.1), 100k steps, best @ step 52000

---

## 1. 项目背景与架构

CalliReader 是一个基于 **InternVL2.5-8B** 的书法 OCR 系统，核心 pipeline 为：

```
整页书法图像 → YOLO 检测 → ViT 特征提取 → Perceiver Resampler → VQ 量化 → InternLM2 LLM 解码
```

- **YOLO**：检测单字/文本行位置
- **ViT**：将检测区域编码为视觉特征
- **Perceiver Resampler**：将 ViT patch tokens 压缩为固定数量的 learnable query tokens
- **VQ（Vector Quantization）**：将 Resampler 输出映射为离散的视觉 token
- **LLM（InternLM2-8B）**：根据视觉 token 生成 OCR 文本

**关键超参数**：
- 原始 CalliReader：`num_layers=4`, `num_learns=3`
- CaoshuReader（草书训练）：`num_layers=8`, `num_learns=12`
- `num_image_token=3`：表示 ViT patch 数量（pixel shuffle 后），与 Resampler query 数量无关

---

## 2. 训练过程总结

### 2.1 数据集
- **CaoshuCC-20k**：约 20,000 张草书单字图像，用于训练 Resampler 的草书写法对齐能力
- 训练目标：让 Resampler 将草书视觉特征更好地映射到 LLM 语义空间

### 2.2 训练曲线
- 总步数：100,000 steps
- 最佳验证损失：`val_loss = 0.8391` @ step 52,000（`caoshu_best_val_1.pt`）
- 训练损失在 ~30k steps 后趋于平稳，后续 70k steps 仅有微小改善
- 最终损失：`caoshu_final.pt`（step 100k），val_loss 略高于最佳点

### 2.3 单字分类基准（CCC Test Set, 200 samples）

| 模型 | 准确率 | 备注 |
|------|--------|------|
| Original CalliReader | 10.5% | 基线 |
| CaoshuReader | 7.0% | **低于基线** |

⚠️ **关键发现**：单字分类任务与 CalliReader 的设计目标不符。CalliReader 是整页 OCR 系统，依赖 YOLO 检测 + LLM 语言建模的联合能力，而非单字孤立分类。单字测试无法反映 Resampler 对整页上下文理解的影响。

---

## 3. 整页 OCR 评估（核心实验）

### 3.1 评估方法
- **测试集**：`examples/` 目录下的 15 张整页书法作品（含真书、行书、草书）
- **Ground Truth**：`answer.txt` 人工标注
- **指标**：CER（Character Error Rate）= Levenshtein 距离 / GT 长度
- **Prompt**：`"读出图中所有文字"`
- **OOM 修复**：`export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`

### 3.2 整体结果

| 模型 | 平均 CER | 成功/总数 | 备注 |
|------|----------|-----------|------|
| Original CalliReader | **81.46%** | **15/15** | 全部成功 |
| CaoshuReader (best_val) | 875.56%* | 9/15 | 6 张 OOM |

*注：CaoshuReader 的平均 CER 被 OOM 的 6 张图（惩罚值 999%+）严重拉高，不具备可比性。*

### 3.3 共同成功样本对比（9 张）

使用加权 CER（按 GT 长度加权）：

| 模型 | 加权 CER | 相对改善 |
|------|----------|----------|
| Original | **99.52%** | — |
| CaoshuReader | **93.78%** | **-5.7%** ✅ |

**逐样本明细**：

| 图像 | GT 开头 | Original CER | Caoshu CER | Δ | 优胜 |
|------|---------|--------------|------------|---|------|
| 2_2.jpg | 雲龍遠飛駕… | 90.0% | 90.0% | 0.0% | ➖ |
| 2_3.jpg | 雲龍遠飛駕… | 160.0% | **150.0%** | -10.0% | ✅ Caoshu |
| 2_4.jpg | 雲龍遠飛駕… | 160.0% | **150.0%** | -10.0% | ✅ Caoshu |
| 7.jpg | 素心愛雲水… | 115.0% | 115.0% | 0.0% | ➖ |
| 10.jpg | 清風明月… | 81.2% | 81.2% | 0.0% | ➖ |
| 11.jpg | 海內存知己… | 90.0% | 90.0% | 0.0% | ➖ |
| 13.jpg | 三合也紙墨… | 109.8% | **82.9%** | **-26.8%** | ✅ **Caoshu** |
| 14.jpg | 天涯落乎… | 85.7% | **83.3%** | -2.4% | ✅ Caoshu |
| 20.jpg | 雁门关外… | **82.0%** | 86.0% | +4.0% | ✅ Original |

---

## 4. 逐样本深度分析

### 4.1 成功案例：6.jpg（Original 最佳）
- **GT**：`国破山河在城春草木深...唐杜甫春望甲辰年王琦书`
- **Original**：`国破山河在城春草木深...唐杜甫诗甲辰年王琦` — CER 仅 **5.9%**，几乎完全正确
- **Caoshu**：OOM ❌

### 4.2 显著提升：13.jpg（CER ↓ 26.8%）
- **GT**：`三合也紙墨相發四合也偶然欲書五合也...`
- **Original**：`三合也纸墨相发四合同偶然欲书五字心迹遽落笔秋之出年今如山更迭风烟日炎凉二米家意气违势屈一叶地之年奔走不殊余亦为异乡客耳`
  - 严重幻觉，混入大量无关文本（"二米家"、"奔走不殊余亦为异乡客耳"）
- **Caoshu**：`三合也纸相发四合同偶然欲书五字合并如山口遽落秋之出冬今亦山口更添留一乖意速势屈二截风煤日炎辛亥岁末李墨不`
  - 虽然仍有错误，但幻觉内容大幅减少，结构更接近 GT
  - **Caoshu Resampler 对密集草书文本的局部特征对齐能力更强**

### 4.3 中等提升：2_3.jpg / 2_4.jpg（CER ↓ 10%）
- **GT**：`雲龍遠飛駕 天馬自行空`
- **Original**：`云影远飞鸳天与鸟自行书己亥秋日木鱼石室`（大量错误）
- **Caoshu**：`云影远飞鸳天鸟白行空己亥秋日木鱼石书`（"白行空"比"与鸟自行"更接近 GT）

### 4.4 持平案例：7.jpg（经典幻觉）
- **GT**：`素心愛雲水此日東南行笑解塵纓處滄浪無限清`
- **Original & Caoshu**：`春色满园关不住一枝红杏出墙来...`
  - 两张模型均完全忽略图像内容，生成常见古诗《游园不值》
  - **这是 LLM 的强先验导致，Resampler 训练无法改变**

### 4.5 原模型更优：20.jpg（CER ↑ 4%）
- **GT**：`...为问西风几时来...`
- **Original**：`为问关西猛士谁五十三军今所从汉兵一百八十八`（开头错误，后续幻觉）
- **Caoshu**：`为问西风几时来不道征人五马回汉家烟尘一百八十年...`
  - 开头 6 字完全正确（"为问西风几时来"）！
  - 但后续继续幻觉（"汉家烟尘一百八十年"）
  - **Caoshu 对草书开头的识别更准确，但 LLM 的续写幻觉更严重**

---

## 5. 关键问题诊断

### 5.1 LLM 幻觉（Hallucination）—— 最主要瓶颈

**现象**：
- 7.jpg：完全生成《游园不值》（"春色满园关不住..."），与 GT（"素心愛雲水..."）无关
- 20.jpg：正确识别开头后，LLM 续写完全不同的古诗（"汉家烟尘..."）
- 10.jpg：生成常见对联"清风明月本无价"，而非 GT"清风明月谁与共"

**根因分析**：
1. **InternLM2 的强语言先验**：8B 参数的语言模型在书法 OCR 语料上未经微调，倾向于生成训练数据中高频出现的经典诗句
2. **Resampler 仅压缩视觉信息，不约束语义**：即使 Resampler 正确提取了"素心愛雲水"的视觉特征，LLM 仍可能因概率更高而输出"春色满园关不住"
3. **Prompt 过于简单**：`"读出图中所有文字"` 没有提供足够的上下文约束

### 5.2 CUDA OOM（CaoshuReader）

**现象**：
- CaoshuReader（8层/12query）在 6 张图上 OOM，原模型（4层/3query）全部成功
- OOM 发生在高分辨率图像的 Resampler 前向传播或 LLM 解码阶段

**根因**：
1. **Resampler 参数量增加**：`4层/3query → 8层/12query`，参数量增加约 4 倍
2. **高分辨率图像产生更多 ViT patch tokens**：复杂图像经 YOLO 检测后生成大量候选框，每个框都过一遍 ViT + Resampler
3. **LLM KV-Cache 占用**：长文本解码时，InternLM2-8B 的 KV-Cache 消耗大量显存

### 5.3 重复生成 / 循环输出

**现象**：
- 部分输出出现无限重复，如 `"汉兵一百八十八...汉兵一百八十八..."`

**根因**：
- `repetition_penalty=1.0`（无惩罚）
- LLM 在缺乏清晰视觉约束时，陷入高频短语的自循环

### 5.4 训练目标与推理任务不匹配

- **训练**：单字草书图像 → Resampler 输出与 GT 对齐（像素级 / 特征级损失）
- **推理**：整页图像 → YOLO 检测 → 多区域聚合 → LLM 生成连贯文本
- **Gap**：训练未涉及 YOLO 检测、多区域聚合、LLM 文本生成等推理阶段的关键环节

---

## 6. 优化建议

### 6.1 短期：缓解 OOM 与重复生成

| 措施 | 预期效果 | 实施难度 |
|------|----------|----------|
| `repetition_penalty=1.3~1.5` | 消除循环输出 | ⭐ 极易 |
| `max_new_tokens=256` | 限制生成长度，减少 KV-Cache | ⭐ 极易 |
| `torch.cuda.empty_cache()` 在每张图后 | 释放显存碎片 | ⭐ 极易 |
| 降低 Resampler 至 `6层/8query` | 平衡性能与显存 | ⭐⭐ 需重新训练 |

### 6.2 中期：Prompt 工程与解码策略

| 措施 | 预期效果 | 实施难度 |
|------|----------|----------|
| Few-shot Prompting | 给 LLM 提供书法 OCR 示例，约束输出格式 | ⭐⭐ 容易 |
| System Prompt + 角色设定 | `"你是一位古籍 OCR 专家，请严格按图中文字转录，不要添加任何解释"` | ⭐ 极易 |
| Constrained Decoding | 限制输出为常见汉字集，避免生僻幻觉字 | ⭐⭐⭐ 中等 |
| Beam Search / top-p 调参 | `num_beams=3`, `top_p=0.9` 可能减少幻觉 | ⭐ 极易 |

### 6.3 长期：端到端微调 LLM（e-IT / LoRA）

**这是最关键、最可能带来质变的方向。**

当前瓶颈明确在 **LLM 的语言先验过强**，而非 Resampler 的视觉编码。建议：

#### 方案 A：LoRA 微调 InternLM2（推荐）
- 冻结 ViT、Resampler、YOLO，仅对 LLM 添加 LoRA 适配器
- 训练数据：整页书法图像 + GT 文本（约 1000~5000 对）
- 目标：让 LLM 学会"看到什么就说什么"，而非背诵古诗
- 预期 CER 降幅：**30~60%**

#### 方案 B：Full Fine-tuning（需要更多资源）
- 解冻 LLM 全部参数，使用书法 OCR 专用语料微调
- 需要 A100 40GB 或更高显存
- 效果可能优于 LoRA，但成本显著增加

#### 方案 C：两阶段训练
1. **Stage 1**（已完成）：Resampler 单字预训练（CaoshuCC-20k）
2. **Stage 2**：冻结 Resampler，LoRA 微调 LLM 的整页 OCR 能力
3. **Stage 3**（可选）：联合微调 Resampler + LLM

### 6.4 数据层面

| 措施 | 说明 |
|------|------|
| 扩充整页训练数据 | 当前仅 15 张测试图，无整页训练数据。需要收集 1000+ 整页书法图像对 |
| 困难样本挖掘 | 针对 13.jpg、20.jpg 等 Caoshu 优于 Original 的样本，分析其视觉特征差异 |
| 多字体覆盖 | 训练数据仅草书，但测试集含真书、行书。需扩充字体种类 |

---

## 7. 结论

1. **CaoshuReader 的 Resampler 训练是有效的**：在共同成功样本中，加权 CER 从 99.52% 降至 93.78%（相对改善 5.7%），在 13.jpg 上提升高达 26.8%。

2. **Resampler 不是当前瓶颈**：CER 仍然高达 ~90%，主要问题是 LLM 的强语言先验导致的幻觉。7.jpg 的案例表明，即使 Resampler 正确提取了视觉特征，LLM 仍会输出概率更高的常见诗句。

3. **OOM 限制了 CaoshuReader 的实用性**：8层/12query 的 Resampler 在 RTX 3090 上无法处理复杂图像，需要降参数量或换用更大显存 GPU。

4. **下一步最关键的动作**：**使用 LoRA 微调 InternLM2-8B**，让语言模型适配书法 OCR 任务。这比继续增大 Resampler 更有可能带来质变。

---

## 附录：原始输出对比

### 6.jpg（Original 最佳，CER 5.9%）
```
GT:  国破山河在城春草木深感时花溅泪恨别鸟惊心烽火连三月家书抵万金
     白头搔更短浑欲不胜簪唐杜甫春望甲辰年王琦书
Pred: 国破山河在城春草木深感时花溅泪恨别鸟惊心烽火连三月家书抵万金
      白头搔更短浑欲不胜簪唐杜甫诗甲辰年王琦
```

### 13.jpg（Caoshu 最佳提升，CER ↓ 26.8%）
```
GT:   三合也紙墨相發四合也偶然欲書五合也心遷體留一乖也意違勢屈...
Orig: 三合也纸墨相发四合同偶然欲书五字心迹遽落笔秋之出年今如山
      更迭风烟日炎凉二米家意气违势屈一叶地之年奔走不殊余亦为异乡客耳
Cao:  三合也纸相发四合同偶然欲书五字合并如山口遽落秋之出冬今亦山口
      更添留一乖意速势屈二截风煤日炎辛亥岁末李墨不
```

### 20.jpg（Caoshu 开头正确，后续幻觉）
```
GT:   ...浮云一百八盘萦为问西风几时来不教黄叶自惊回...
Orig: 为问关西猛士谁五十三军今所从汉兵一百八十八
Cao:  为问西风几时来不道征人五马回汉家烟尘一百八十年谁将黄鹤换燕支
```

### 7.jpg（双双幻觉，LLM 瓶颈）
```
GT:   素心愛雲水此日東南行笑解塵纓處滄浪無限清
Both: 春色满园关不住一枝红杏出墙来无题诗范仲淹句为宏/容
```
