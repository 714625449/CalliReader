# CaoshuReader 项目日志 — 2026-05-10

> 本日志记录对 CaoshuReader 项目的整体理解、显存优化实施、问题排查及后续计划。

---

> **环境说明**：本项目所有依赖均安装在 `conda env caoshu` 中（`/root/miniconda3/envs/caoshu`），训练/测试请务必在此环境下运行，以确保 FlashAttention 等关键库可用。

## 一、项目理解

### 1.1 项目目标

**CaoshuReader** 是一个基于深度学习的**草书（中国书法）单字识别系统**。目标是：
- **输入**：一张书法作品的整图扫描件
- **输出**：识别出图中每一个草书单字，并按阅读顺序排列成文本

### 1.2 核心架构

系统由两个阶段组成：

| 阶段 | 模块 | 功能 | 状态 |
|------|------|------|------|
| **阶段1：整图分割** | YOLO | 检测原图中的单字位置，输出 bbox | 冻结 |
| **阶段2：单字识别** | ViT + MLP1 + PerceiverResampler + LLM Embedding | 提取单字视觉特征，与文本嵌入对齐，通过余弦相似度识别字符 | Resampler 可训练，其余冻结 |

**详细模块说明：**

| 模块 | 参数规模 | 角色 |
|------|---------|------|
| **InternViT** | ~580M | 视觉特征提取器，输入 448×448，输出 patch tokens |
| **MLP1** | ~65M | 视觉投影层，将 ViT 输出投影到 4096 维 LLM 空间 |
| **PerceiverResampler** | **0.571B**（当前运行）/ 1.141B（设计目标） | 可训练核心，4 层 Transformer，3 个 learnable queries，将视觉特征压缩为固定长度表示 |
| **LLM Token Embedding** | ~724M | 冻结的文本嵌入层，提供字符的语义向量 |
| **Tokenizer** | — | InternLM2 tokenizer，将字符转为 token id |

### 1.3 训练目标

训练只优化 **PerceiverResampler**（**0.571B** 参数，4 层 × 3 queries），其余模块全部冻结。

**损失函数**（alignment loss）：
```python
tgt_norm = F.normalize(target_embed, dim=-1).unsqueeze(1)   # (B, 1, D)
pred_norm = F.normalize(pred, dim=-1)                       # (B, N, D)
cosine_sim = (pred_norm * tgt_norm).sum(dim=-1)             # (B, N)
loss = (1 - cosine_sim).mean()                              # scalar
```

即：让每个 learnable query 的输出与目标字符的 embedding 之间的**余弦相似度最大化**。

**推理方式**：
1. 输入图片 → ViT → MLP1 → Resampler → 输出 (B, 3, 4096)
2. 对 3 个 query 做 mean pooling → (B, 4096)
3. 与预计算的所有字符 embedding 计算余弦相似度
4. 取相似度最高的字符作为预测结果

### 1.4 项目文件结构

```
caoshu/
├── caoshu/
│   ├── train.py          # 单字训练（只训练 Resampler）
│   ├── pipeline.py       # 整图识别 Pipeline（YOLO + Resampler）
│   ├── visualizer.py     # YOLO 检测可视化
│   └── dataset.py        # 数据集加载
├── test_caoshu.py        # 验证集准确率测试
├── models/
│   ├── model.py          # 模型加载工具函数
│   ├── perceiver_resampler.py  # PerceiverResampler 定义
│   └── similarity.py     # 相似度计算
├── config/
│   └── configu.py        # 路径和训练配置
├── InternVL/             # InternVL 模型代码 + 权重（~15GB，软链接）
├── params/               # 训练参数（~6GB，软链接）
│   ├── callialign.pth    # Resampler checkpoint（3.2GB）
│   ├── vit_model.pt      # ViT 权重
│   ├── mlp1.pth          # MLP1 权重
│   ├── token_embedding.pth / gauss_norm.pth  # 文本嵌入
│   ├── best.pt           # YOLO 模型
│   └── orderformer.pth   # 阅读顺序排序模型
├── imgs/                 # 整篇书法作品图片，用于 Pipeline 端到端测试
│   ├── 2.jpg ~ 14.jpg    # 示例整图
│   └── ...
└── ...

**外部数据路径：**
- `/root/sj-tmp/datasets/CCC_split/` — 单字训练数据集（Training / Validation / Test）
```

---

## 二、今日工作（2026-05-10）

### 2.1 显存优化（已完成）

**问题**：训练、测试、Pipeline 三阶段均出现 OOM。

**根因**：
- 训练/测试：ViT 中间激活被保留供反向传播，显存占用大
- Pipeline：YOLO 直接读取超高分辨率原图

**优化措施**：

| 文件 | 修改 |
|------|------|
| `caoshu/train.py` | 删除 `GradScaler`（bf16 不需要）；冻结模块包 `torch.no_grad()` + `autocast(dtype=torch.bfloat16)`；输入不再手动 `.to(bfloat16)` |
| `test_caoshu.py` | 全部前向在 `no_grad` + `autocast(bf16)` 下；出 autocast 后 `.float()` 避免 dtype 冲突 |
| `caoshu/pipeline.py` | 暴露 `--yolo_imgsz=1344`；YOLO 内部 letterbox 保长宽比；从原图裁字（bbox 映射回原图坐标） |
| `caoshu/visualizer.py` | `detect_and_visualize()` 支持 `imgsz=` 参数 |

### 2.2 路径修复（已完成）

服务器重装后项目从 `/workspace/CalliReader` 迁移到 `/caoshu`。

实际路径已通过**软链接**配置好，无需修改 `configu.py`：
- `/caoshu/InternVL` → `/root/sj-tmp/callireader_models/InternVL`
- `/caoshu/params` → `/root/sj-tmp/callireader_models/params`

修改了 `train.py`、`test_caoshu.py`、`pipeline.py` 中的 `PROJECT_ROOT`。

### 2.3 模型结构自推断（已完成）

**问题**：checkpoint 包含 `module.` 前缀（DDP 训练残留），直接加载报 `Unexpected key(s) in state_dict`。例如 key 为 `module.layers.0.0.norm_media.weight`，但模型期望 `layers.0.0.norm_media.weight`。

**修复**：
1. 加载前移除 `module.` 前缀：`state_dict = {k.replace('module.', ''): v for k, v in state_dict.items()}`
2. 为兼容未来可能的不同规模 checkpoint，保留从 state_dict 自推断 `num_layers` / `num_learns` 的能力。

已修复文件：`test_caoshu.py`、`pipeline.py`、`train.py`。

### 2.4 pipeline.py 重大 bug 修复（已完成）

**问题**：`_compute_all_char_embeddings()` 直接用 `torch.arange(0, num_classes)` 作为索引查 `tok_embeddings`。

**错误原因**：`dataset.idx2char` 的索引（0,1,2...）**不等于 tokenizer 的 token id**。例如 `idx2char[0]='一'`，但 tokenizer 把 '一' 编码为另一个 token id。

**后果**：预计算的 `all_char_embeds` 根本不是字符的 embedding，整图识别的相似度比较完全错误。

**修复**：通过 tokenizer 把字符正确转成 token id：
```python
batch_tokens = tokenizer(batch_chars, ...).input_ids[:, 0]
embeds = tok_embeddings(batch_tokens)
```

### 2.5 train.py 冒烟测试（已完成）

```bash
cd /caoshu && python caoshu/train.py \
    --data_root=/root/sj-tmp/datasets/CCC_split \
    --save_dir=/tmp/train_smoke \
    --total_steps=10 --batch_size=4 --grad_accum=2
```

**结果**：
- ✅ 正常启动，本地 InternVL 配置加载成功（无需网络）
- ✅ 显存峰值：**3.54 GB** / 23.68 GB（base 环境，无 FlashAttention）
- ✅ loss 从 1.00 降至 0.88（10 步），训练逻辑正常

> 注：切换至 `conda env caoshu` 启用 FlashAttention 后，同配置显存降至 **3.74 GB**（batch_size=16）。

### 2.6 准确率问题根因排查（已完成）

**现象**：`test_caoshu.py` 在 Validation 集上准确率 **0%**，所有样本预测为同一个字"逄"。

**诊断脚本**：`/caoshu/debug_acc.py`

**核心发现：模型输出完全 collapse**

| 指标 | 数值 | 说明 |
|------|------|------|
| 样本间 pred 差异 | **< 0.02** | 不同图片输入产生几乎相同的输出 |
| 单个样本内 learns 差异 | **~0.025** | 12 个 query 输出几乎相同 |
| Top5 相似度值 | **完全相等** | pred 向量与多个 embedding 的相似度精确相等 |

**结论**：
- checkpoint `caoshu_best.pt` (step 47339, loss 0.8047) **完全不可用**
- 模型发生了 **representation collapse**，所有输入被映射到特征空间的同一点
- 这不是代码 bug，而是**该 checkpoint 本身没有训练好**

### 2.7 `callialign.pth` 实际可用性验证（已完成）

之前误以为 `callialign.pth` 与 `caoshu_best.pt` 是同一 collapse checkpoint。实际测试后发现：

| Checkpoint | Step | Loss | Validation 准确率 |
|-----------|------|------|------------------|
| `callialign.pth` | 50000 | 0.0860 | **29%**（100 样本） |
| `caoshu_best.pt` | 47339 | 0.8047 | 0%（collapse） |

**结论**：`callialign.pth` **可用**，学到了草书特征，但可能过拟合，建议降低 lr 继续微调。

### 2.8 `test_caoshu.py` bug 修复（已完成）

测试 `callialign.pth` 时发现两个代码 bug：

| Bug | 现象 | 修复 |
|-----|------|------|
| DDP `module.` 前缀未处理 | `RuntimeError: Missing key(s) in state_dict` | 加载前 `k.replace('module.', '')` |
| checkpoint 键名不兼容 | `ValueError: Unknown format code 'f' for object of type 'str'` | `ckpt.get('step', ckpt.get('total_step'))` + 类型安全格式化 |

### 2.9 迁移至 `conda env caoshu` + FlashAttention 启用（已完成）

**发现**：`flash-attn==2.8.3` 已安装在 `conda env caoshu` 中，但之前一直在 base 环境运行，导致 fallback 到普通 attention。

**操作**：
- 所有训练/测试命令前缀增加 `conda activate caoshu`
- 训练显存从 **5.17 GB** → **3.74 GB**（节省 1.4GB）
- 速度提升（FlashAttention 原生优化）

### 2.10 PyTorch FutureWarning 清理（已完成）

| 警告 | 涉及文件 | 修复 |
|------|---------|------|
| `torch.cuda.amp.autocast` deprecated | `train.py`×2, `test_caoshu.py`, `debug_acc.py` | 替换为 `torch.amp.autocast('cuda', ...)` |
| `torch.load` 未指定 `weights_only` | `models/model.py`×8, `pipeline.py`, `test_caoshu_safe.py`, `get_single_embeddings.py` | 对权重文件用 `True`，对 checkpoint 明确 `False` |

### 2.11 `requirements.txt` 更新（已完成）

新增 `flash-attn==2.8.3`。

---

## 三、遇到的问题与解决

| # | 问题 | 现象 | 解决方案 |
|---|------|------|---------|
| 1 | OOM（训练/测试） | ViT 激活保留导致显存溢出 | 冻结模块包 `torch.no_grad()` + `autocast(bf16)` |
| 2 | OOM（Pipeline） | YOLO 读取超高分辨率原图 | YOLO 原生 `imgsz=1344` + letterbox |
| 3 | 路径错误 | 硬编码 `/workspace/CalliReader` 不存在 | 改为 `/caoshu`；确认软链接已配置 |
| 4 | DDP `module.` 前缀 | `Unexpected key(s) in state_dict` | 加载前 `k.replace('module.', '')` |
| 5 | autocast dtype 冲突 | `RuntimeError: expected scalar type BFloat16 but found Float` | 出 autocast 后显式 `.float()` |
| 6 | pipeline token id 错误 | 预计算 embedding 使用错误索引 | 通过 tokenizer 把字符转为正确 token id |
| 7 | learns 设备不匹配 | `Expected all tensors on same device, cpu and cuda:0` | 创建 Parameter 时显式指定 `device` 和 `dtype` |
| 8 | **准确率 0%** (`caoshu_best.pt`) | 所有样本预测为同一个字 | **根因：`caoshu_best.pt` 完全 collapse，需重新训练；`callialign.pth` 可用（29%）** |

---

## 四、当前状态

### 4.1 代码状态

| 文件 | 状态 |
|------|------|
| `caoshu/train.py` | ✅ 可正常运行，显存优化完成，支持 resume 自推断，autocast 已更新 |
| `test_caoshu.py` | ✅ 可正常运行，模型结构自推断 + dtype 转换 + DDP 前缀修复 |
| `caoshu/pipeline.py` | ✅ 代码逻辑已修正（token id + 原图裁字 + YOLO imgsz + weights_only） |
| `caoshu/visualizer.py` | ✅ YOLO imgsz 参数已添加 |
| `config/configu.py` | ✅ 路径正确 |
| `models/model.py` | ✅ `torch.load` weights_only 已明确指定 |
| `requirements.txt` | ✅ 已补充 `flash-attn==2.8.3` |

### 4.2 模型状态

| Checkpoint | 路径 | 状态 | 备注 |
|-----------|------|------|------|
| `callialign.pth` (step 50000, loss 0.0860) | `/caoshu/params/callialign.pth` | ✅ **可用**，Validation 准确率 **29%**（100样本） | 学到了草书特征，但可能过拟合，建议降低 lr 继续微调 |
| `caoshu_best.pt` (step 47339, loss 0.8047) | `/root/sj-tmp/checkpoints/CaoshuReader/caoshu_best.pt` | ❌ **不可用**，输出 collapse | 所有样本预测为同一个字"逄"，representation collapse |
| `best.pt` (YOLO) | `/caoshu/params/best.pt` | ✅ 可用 | 整图单字检测正常 |
| `orderformer.pth` | `/caoshu/params/orderformer.pth` | 未验证 | 阅读顺序排序模型 |

### 4.3 数据集状态

| Split | 类别数 | 样本数 |
|-------|-------|-------|
| Training | 8398 | 338,870 |
| Validation | 7440 | 61,181 |
| Test | — | 14,756 |

注意：Validation 比 Training 少约 1000 类，可能为 Training 子集。

---

## 五、下一步计划

### 5.1 训练监控与调优（进行中）

#### 第一轮：lr=1e-4（已停止）

**状态**：运行至 step 13000+ 后停止。

| 指标 | 数值 | 说明 |
|------|------|------|
| 显存占用 | ~8 GB / 23.68 GB | FlashAttention 启用后显著降低 |
| Best Loss | **0.6634 @ step 51** | 极低步数即达到最佳，之后未再下降 |
| 当前 Loss | 0.73 ~ 0.85 | step 10000+ 后持续震荡 |
| 已保存 ckpt | step 11000, 12000, ... | 自动轮转，保留最近 10 个 |

**问题判断**：❌ **Loss 在 step 51 后不再下降，持续震荡** → 学习率过高（`lr=1e-4`）。

#### 第二轮：lr=5e-5（当前运行中）

**启动命令**：
```bash
cd /caoshu && conda activate caoshu && python caoshu/train.py \
    --data_root=/root/sj-tmp/datasets/CCC_split \
    --save_dir=/root/sj-tmp/checkpoints/CaoshuReader_lr5e5 \
    --total_steps=100000 \
    --batch_size=16 \
    --grad_accum=16 \
    --lr=5e-5 \
    --save_every=1000 \
    --keep_ckpts=10
```

**当前进展**（实时）：
| Step | Loss | 状态 |
|------|------|------|
| 1 | 1.0010 | 启动正常 |
| 23 | 0.9106 | 持续下降 |
| 69 | 0.8081 | **仍在下降，未出现之前 lr=1e-4 的触底反弹** ✅ |

**初步结论**：降低 lr 有效，loss 曲线恢复正常下降趋势。

**重要说明**：`callialign.pth` 训练的是**正楷+草书混合数据**，正楷 loss 极低（易识别），拉低了整体 loss 至 0.086。当前 `CCC_split` 数据集为**纯草书**（样本如图，笔画高度连笔、变形严重），识别难度远高于正楷，**不可直接对比 loss 绝对值**。

**监控指标**：
- **每 5000 步用 `test_caoshu.py` 测 Validation 准确率** — 这是唯一可靠指标
- **目标**：超越 `callialign.pth` 在纯草书上的 Validation 准确率（当前未知，需测试）
- loss 只作为相对趋势参考（看是否持续下降，不看绝对值）
- 若 10000 步后 loss 不再下降或准确率不提升，再考虑降至 1e-5 或加入 cosine annealing

### 5.2 `callialign.pth` Pipeline 端到端验证（已完成）

使用 `callialign.pth` 对 `imgs/2.jpg` 进行整图识别测试：

| 项目 | 结果 |
|------|------|
| YOLO 分割 | ✅ 检测到 20 个字符，bbox 位置合理 |
| Top-1 置信度 | 33% ~ 38%（普遍偏低） |
| Top-3 分布 | 候选间分数极接近（如 悌34% \| 差33% \| 天33%） |
| 阅读顺序 | 正常 |

**结论**：
- 模型确实学到了草书特征（Top-3 常含正确字形），但 **Top-1 区分度不足**
- 这与 Validation 准确率 29% 一致——模型"认识"字，但不够"确定"
- 训练目标：提升 Top-1 置信度和准确率

### 5.3 待验证（新 checkpoint）

待训练出优于 `callialign.pth`（29%）的 checkpoint 后：
1. 用 `test_caoshu.py` 验证单字识别准确率
2. 用 `pipeline.py` 再次验证整图识别效果

### 5.3 技术债

| 事项 | 优先级 |
|------|-------|
| ~~`torch.cuda.amp.autocast` → `torch.amp.autocast('cuda', ...)`~~ | ~~已完成~~ ✅ 已修复 `caoshu/train.py`、`test_caoshu.py`、`debug_acc.py` 共 4 处 |
| ~~`torch.load` 的 `weights_only` 明确指定~~ | ~~已完成~~ ✅ 已修复 `models/model.py`、`pipeline.py`、`test_caoshu_safe.py`、`get_single_embeddings.py` 共 12 处，消除 PyTorch 2.6+ FutureWarning |
| ~~FlashAttention 安装~~ | ~~已完成~~ ✅ 已在 `conda env caoshu` 中启用（`flash-attn==2.8.3`），显存从 5.17GB 降至 3.74GB |

---

## 六、快速参考

### 训练
```bash
cd /caoshu && conda activate caoshu && python caoshu/train.py \
    --data_root=/root/sj-tmp/datasets/CCC_split \
    --save_dir=/root/sj-tmp/checkpoints/CaoshuReader \
    --total_steps=100000 --batch_size=16 --grad_accum=16 --lr=1e-4
```

### 测试准确率
```bash
cd /caoshu && conda activate caoshu && python test_caoshu.py \
    --ckpt=/caoshu/params/callialign.pth \
    --data_root=/root/sj-tmp/datasets/CCC_split \
    --split=Validation --num_test=100 --batch_size=16
```

### 整图识别
```bash
cd /caoshu && conda activate caoshu && python caoshu/pipeline.py \
    --image=imgs/2.jpg \
    --ckpt=/caoshu/params/callialign.pth \
    --data_root=/root/sj-tmp/datasets/CCC_split \
    --output=outputs/pipeline --yolo_imgsz=1344
```

---

## 七、e-IT LoRA 微调记录（2026-05-11）

### 7.1 背景
在 Resampler 训练完成后，使用 539 个纯草书样本对 InternVL 的 Language Model 进行 e-IT（Embedding-based Instruction Tuning）LoRA 微调。

### 7.2 训练配置
- **数据**: `/caoshu/train/samples_eit.json`（539 样本）
- **模型**: InternVL（本地 `/caoshu/InternVL`）
- **微调范围**: 仅 `language_model`，LoRA r=128, alpha=256
- **目标模块**: wqkv, wo, w1, w2, w3
- **优化器**: AdamW8bit（节省显存）
- **学习率**: 5e-5
- **精度**: bf16
- **Epoch**: 10
- **Batch size**: 1
- **冻结**: vision_model, resampler, mlp1, normed_emb

### 7.3 训练历程

#### 第一轮（脚本: `eit_train_simple.py`）
| 时间 | 事件 |
|------|------|
| 5/10 | 编写轻量级训练脚本，绕过 xtuner |
| 5/10 | 尝试 bf16 + LoRA(r=128)，`optimizer.step()` OOM |
| 5/10 | 改用 AdamW8bit，成功启动训练 |
| 5/10 | 训练至 Step 700，保存 checkpoint-700 时磁盘满失败 |

#### 第二轮（脚本: `eit_train_resume.py`）
| 时间 | 事件 |
|------|------|
| 5/11 ~02:51 | 从 checkpoint-1700 恢复训练 |
| 5/11 ~03:06 | 已跑到 Step 4500+，Epoch 9/10 |

### 7.4 Loss 趋势
- Epoch 1: 平均 1.57
- Epoch 2~3: 降至 0.03~1.1
- Epoch 6: 大部分在 0.01~0.1 之间
- Epoch 8+: 高度收敛

### 7.5 Checkpoint 管理
- 保存路径: `/caoshu/outputs/eit_simple_overfit`（符号链接到 `/root/sj-tmp/eit_simple_overfit`）
- 自动清理: 保留最近 5 个 checkpoint，删除旧的
- 磁盘监控: 低于 5GB 时跳过保存

### 7.6 当前状态（截至 2026-05-11 03:06）
- **最新 checkpoint**: `checkpoint-4500`
- **当前步数**: ~4500+ / 5390（10 epoch）
- **当前 Epoch**: ~9/10
- **Loss**: 0.01~0.1
- **磁盘剩余**: ~34GB

### 7.7 恢复训练命令
```bash
cd /caoshu && /root/miniconda3/envs/caoshu/bin/python scripts/eit_train_resume.py
```

### 7.8 相关文件
- 训练脚本: `/caoshu/scripts/eit_train_resume.py`
- 输出目录: `/caoshu/outputs/eit_simple_overfit`
- 数据: `/caoshu/train/samples_eit.json`
